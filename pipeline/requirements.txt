# Image pipeline dependencies for pipeline/.
# pregen.py needs only Pillow (for reference downscaling); it degrades
# gracefully without it. matte.py needs numpy + scipy (region-based cutout,
# no ML model). build_masks.py needs Pillow. verify.py is standard-library
# only.
Pillow>=10.0
numpy>=1.24
scipy>=1.10
