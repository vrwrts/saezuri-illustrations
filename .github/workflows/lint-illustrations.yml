name: Lint illustrations

# Lints the illustration PNGs a PR changes. Result is surfaced two ways: the job
# summary here, and a sticky PR comment posted by the companion pr-comment.yml.
#
# Why the comment lives in a SEPARATE workflow: a pull_request run from a fork gets a
# READ-ONLY token (a GitHub security rule), so it can't comment. This workflow runs
# the untrusted PR content read-only and just emits a report artifact; pr-comment.yml
# (workflow_run, write token, never checks out PR code) turns it into the comment.
#
# A lint for honest mistakes, not a trust gate — real trust is human review (CONTRIBUTING.md).
on:
  pull_request:
    paths:
      - 'illustrations/**'
      - 'scripts/validate.sh'

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # need the base commit to diff changed images
      - run: pip install --quiet -r pipeline/requirements.txt
      - name: Lint changed PNGs
        run: |
          base='${{ github.event.pull_request.base.sha }}'
          # Expand the changed files to whole species: both poses of every touched slug,
          # so the pairing check sees a missing counterpart — including when a PR deletes
          # one pose (deletions are kept in the diff for exactly that reason). Names that
          # no longer exist in the tree are dropped; deleting both poses lints nothing.
          mapfile -t pngs < <(
            git diff --name-only "$base" HEAD -- 'illustrations/*.png' \
              | sed -E 's#(-2)?\.png$##' | sort -u \
              | while read -r slug; do
                  for f in "$slug.png" "$slug-2.png"; do [ -e "$f" ] && echo "$f"; done
                done
          )
          echo '${{ github.event.pull_request.number }}' > pr-number
          # A hidden marker so the comment workflow can find + update its own comment.
          marker='<!-- illustration-lint -->'
          n=${#pngs[@]}
          if [ "$n" -eq 0 ]; then
            printf '%s\nThis pull request changes no illustrations.\n' "$marker" > report.md
            cat report.md >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          set +e
          tsv="$(bash scripts/validate.sh --tsv "${pngs[@]}")"
          rc=$?
          set -e
          {
            echo "$marker"
            echo "This pull request checks **$n** image(s):"
            echo
            echo '| Filename | Status | Comment |'
            echo '| --- | :---: | --- |'
            while IFS=$'\t' read -r status file msg; do
              [ -n "$file" ] || continue
              case "$status" in
                pass) emoji='✅' ;;
                warn) emoji='⚠️' ;;
                fail) emoji='❌' ;;
                *) emoji="$status" ;;
              esac
              msg=${msg//|/\\|} # escape pipes so a message can't break the table
              echo "| \`$file\` | $emoji | $msg |"
            done <<< "$tsv"
          } > report.md
          cat report.md >> "$GITHUB_STEP_SUMMARY"
          exit "$rc" # fail the check on errors (warnings pass)
      - name: Upload report for the comment workflow
        if: always() # also upload when the lint step failed, so the comment shows the errors
        uses: actions/upload-artifact@v4
        with:
          name: lint-report
          path: |
            report.md
            pr-number
          retention-days: 3
